/** Max pasted input per textarea / field for API cost protection (tools + generate API). */
export const TOOL_INPUT_MAX_CHARS = 2000;

/** Max optional "extra instructions" on generate / version (prompt-injection surface). */
export const EXTRA_INSTRUCTIONS_MAX_CHARS = 600;
